enum JumpAlign {
  START = "start",
  CENTER = "center",
  END = "end",
}

enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
  CUSTOM = "custom",
}

export { JumpAlign, ThemeMode };
